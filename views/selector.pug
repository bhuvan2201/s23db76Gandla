extends layout

block content
  h1= title
  - var image_names = ["apple", "banana", "orange"]
  - for(let i = 0; i < 3; i++) {
    - let number = Math.floor(Math.random() * 3)
        img(src=`/images/${image_names[number]}.jpg`, alt="abc", height="300px", width="300px")
  - }